import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'at.ac.fhstp.sigmund.superquiz201503',
  appName: 'quizSigmund',
  webDir: 'www'
};

export default config;
